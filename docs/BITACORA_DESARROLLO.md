# Bitácora de desarrollo

Registro resumido y acumulativo. Los datos personales y la auditoría de expedientes pertenecen al workspace, no a este documento. Cada entrada indica cambios, pruebas, decisiones y continuación.

## 2026-09-21 · Antecedentes 0.4 / 0.4.1

Se implementaron workspaces aislados, migraciones transaccionales, respaldos completos, recuperación TIFF, visor y atajos, datos ficticios y actualización de código sin copiar datos locales a producción. En 0.4.1 se añadieron validación de manifiestos, recuperación de código condicionada a datos intactos y ensayo automatizado. Resultado final 0.4.1: 114 aprobadas, 8 omitidas en dos perfiles Pillow sobre Python 3.11.4. Ver `VALIDACION_V041.md`. Windows 7 sigue pendiente. Se conservan `.development/original-code.zip`, bundle v0.3 y ZIP 0.4/0.4.1.

## 2026-09-21 · Modelo operativo · Fase 0

Se leyó el nuevo documento y se revisaron esquema, migraciones, Store, TIFF, importadores y UI. Hallazgo: identidad del expediente y ciclo mezcladas en trabajos; sin préstamos validados, entregas inmutables ni revisión por snapshot. Kanban de tareas manuales. Se conserva arquitectura local y se añaden relaciones sin eliminar IDs o archivos. Diagnóstico: `DIAGNOSTICO_MODELO_V04.md`. Baseline: 122 ejecutadas, 114 aprobadas, 8 omitidas, 0 fallos/errores (`.development/baseline-modelo-operativo.json`). Próximo: migración de maestro/préstamo/ciclo, pruebas y servicios de recepción/asignación.

## 2026-09-21 · Fase 1 · Maestro, préstamo y ciclo

Núcleo 3: `operational_schema.sql`, `operational_migration.py` y `operations.py`; integración en Store e importador. Se mantienen IDs de trabajos como ciclos y se enlazan maestros únicos CURP/legajo. Custodia y recepción por elemento, aceptación parcial de folio, asignación/reasignación/colaboración con historial. Datos históricos desconocidos permanecen sin fecha confirmada. No se permite cambiar el folio de un ciclo existente; se crea otro ciclo.

Pruebas sustituidas con justificación: la prohibición de repetir identidad entre folios y la reasignación del folio sobre el mismo trabajo contradicen la nueva regla. Excel sintético ahora conserva tres ciclos de dos maestros; los conteos sin folio que quedan ambiguos se retienen en origen/pendientes. No se infiere legajo 1 de una lista que no lo declara.

Suite: 129 ejecutadas, 121 aprobadas, 8 omitidas, 0 fallos/errores (`.development/fase1-modelo.json`). Siete escenarios nuevos cubren maestro, segundo legajo, lote de 20 con recepción parcial, reasignación, custodia independiente, rol consulta y notas sin cambio de etapa. Respaldo de demo: `.development/respaldo-pre-modelo-operativo`; código anterior: `.development/pre-modelo-operativo-041.zip`.

Interfaz operativa integrada pendiente de Fase 6. Windows pendiente. Próximo: UUID/documentos y versiones TIFF sin pérdida de originales/revisiones.

## 2026-09-22 · Fases 2, 3 y 4 · Identidad TIFF, entregas y revisión

Núcleo 4 añade UUID de documentos/versiones, vínculos de reescaneo y almacenamiento de versiones editadas. Conserva todos los IDs/rutas/revisiones anteriores; nombre original desconocido permanece NULL. Reconstruye `archivos_tiff` para permitir copias idénticas explícitamente autorizadas sin borrar datos, conforme al [procedimiento documentado por SQLite](https://www.sqlite.org/lang_altertable.html#making_other_kinds_of_table_schema_changes); comprueba FK y restaura su activación al terminar. El hash ya no impone la identidad lógica.

Núcleo 5 añade entregas, items, validaciones, sesiones y observaciones con historial. Organizar publica una entrega nueva (si hay inventario), con copias independientes y manifiesto verificable. `manifest_hash` cubre metadatos y contenido; `inventario_hash` permite comparar inventarios entre números/fechas distintos. Carpeta nueva por ciclo/número; una preparación fallida queda Incompleta y no se mezcla con el siguiente intento. Renombrar conserva documento; retirar no borra entregas previas; reescaneo completo conserva el documento y crea versión sucesora.

Revisión ligada a entrega exacta; observación admite documento, versión, página, hoja física y evidencia. Edición TIFF no resuelve observaciones. Respuesta explícita con entrega posterior, validación del revisor y aprobación separadas. Cierre solo mediante aprobación de entrega vigente; custodia permanece independiente. Los estados detallados incluyen correcciones y listo para aprobación.

Pruebas: primera corrida de assets detectó dos errores en el nombre de operación del fixture (`rotate90`); corregidos a la API existente `rotate`. Subsuite assets: 7/7. Regresión integrada Fases 2–4: **150 ejecutadas, 142 aprobadas, 8 omitidas, 0 fallos/errores** (`.development/fases234-modelo.json`). Incluye v1 A/B/C → v2 A/C/D, conservación de v1, sin residuos, hash/catálogo/edición externa, copia interrumpida y revisión completa. Windows pendiente. Próximo: importación histórica y UI/360/Kanban.

## 2026-09-22 · Fase 5 · Importación histórica

`legacy.py` analiza recursivamente sin escribir en origen ni SQLite; registra diagnóstico externo por confirmación o adopta copias verificadas. Detecta estructura, códigos, ubicación, nombres, duplicados/colisiones, ilegibles y páginas desconocidas. No adopta TXT; los informa. Adopción de duplicados es explícita y conserva ambos. Núcleo 6 añade `importacion_items`: cada copia queda vinculada dentro de la transacción de importación, para reanudar sin duplicarla. Fuente/catálogo cambiados exigen repetir el diagnóstico. No se infieren folios de un origen sin evidencia.

Suite: **160 ejecutadas, 152 aprobadas, 8 omitidas, 0 fallos/errores** (`.development/fase5-modelo-final.json`), tras corregir un literal no ASCII de un fixture. Diez casos nuevos, incluido fallo después de copiar y reanudación. Backup/restauración ahora también comprueba las entregas exportadas y sus manifiestos. No se usaron carpetas ni Excel privados.

## 2026-09-22 · Fase 6 · Expediente 360, Kanban e interfaz

`reporting.py`: resumen en cualquier etapa, historial, custodia, asignación, validación, entregas/diff, revisión y exportación HTML imprimible. Inventario aún desconocido se muestra sin dato, no como cero digitalizado. KPI usan archivos activos del ciclo vigente, sin sumar snapshots; reportes CSV viejos se etiquetan declarados. Datasets normalizados y tiempos con cobertura.

`operational_ui.py` integra recepción/préstamo por folio, asignación, 360, entregas, observaciones y legado. Visor de evidencia abre la copia exacta de la entrega, sin edición. Kanban de cinco columnas derivadas; nota/prioridad no cambia etapa. Tareas manuales históricas se conservan como auxiliares. Reglas en servicios; operador/roles locales no equivalen a autenticación ni firma inviolable.

Suite: **169 ejecutadas, 161 aprobadas, 8 omitidas, 0 fallos/errores** (`.development/fase6-modelo.json`). Ventanas reales abiertas automáticamente en Mac: App, visor, 360, préstamo, tablero, entrega y evidencia (`.development/smoke-modelo-operativo.log`). Prueba humana Windows y rendimiento 3 GB pendientes. Se prepara versión 0.5.0; falta cerrar regresión de migración antigua, documentación, demo nueva y paquete distribuible.

## 2026-09-22 · Fase 7 · Cierre del modelo, datasets y traslado 0.5.0

Se completaron manual operativo, arquitectura, esquema/relaciones, migraciones, mapa de 40 aceptaciones y contexto resumido. README, WINDOWS7, RESPALDOS, ENSAYO_OFICINA, LAPTOP y ACTUALIZACION.html describen 0.5.0; la documentación anterior queda marcada como histórica. `AGENTS.md` orienta futuras sesiones a este registro y al contexto. El paquete incluye ambos; no contiene bases, TIFF ni demos.

La demo nueva incorpora recepción/asignación, v1/v2, una observación respondida pendiente de validar y reescaneo pendiente. Está en `ensayos/modelo-operativo-v05/demo`; la repetición final completa vive en `ensayos/modelo-operativo-v05-final`. Exportar entrega crea una carpeta nueva exclusiva y el verificador CLI puede comprobarla sin abrir SQLite; aprobación y devolución física siguen separadas. Se corrigió la recepción agregada al registrar otro ciclo borrador en un folio previamente aceptado. Se añadieron pruebas para extras no relacionados, recepción parcial, exportación y enlace de origen que no debe recorrerse.

Migración antigua: cinco regresiones conservan IDs de trabajos/TIFF/revisiones/conteos, originales, FK y UUID al repetir; simulan fallo tras reconstrucción y columna extra desconocida, con rollback; comprueban la primera edición tras migrar. E2E `.development/e2e_v05.py`/`e2e-v05.log`: código original 0.3 archivado + base/TIFF inventados y rutas Windows → paquete 0.5.0 → recuperación del código previo con SQLite byte a byte y originales intactos → reaplicación → migración núcleo 6/TIFF 3 y entrega verificable → recuperación posterior rechazada, conservando datos/código. No se probó con información de oficina. El paquete estable 0.4.1 se conserva.

Pruebas privadas actualizadas por reglas sustituidas: el KPI 500 de CSV queda en “TIFF declarados en reportes legados”, no en archivos gestionados; reporte legado no permite cierre sin entrega aprobada. No se ejecutaron esas pruebas privadas por ausencia de sus fuentes. El resto se ejecutó con fixtures sintéticos.

## 2026-09-22 · Ampliación solicitada · WAL de Windows y UX de Codificación

El usuario reportó fallos Windows 7 en `test_wal_committed_data_also_blocks_recovery` y `test_backup_wal_and_corruption_refused`: `plain_path` se aplicaba a SHM antes de excluirlo. Cambio mínimo en `backup.py`: filtro léxico de bases/auxiliares antes de resolver/stat; permanece el snapshot API SQLite y la validación de rutas, enlaces/junctions, integridad, hashes y referencias incluidas. No se capturó globalmente PermissionError ni se deshabilitaron pruebas. Dos regresiones nuevas simulan auxiliares bloqueados y prueban que un archivo incluido inaccesible sigue deteniendo el respaldo. Subsuite `test_safety`: 34/34 aprobadas (`.development/wal-fix-safety.log`). Ambas pruebas reportadas pasan localmente con los dos Pillow.

Rediseño en `coding_ui.py`, `coding_ui_v3.py` y nuevo `file_browser.py`: prioridad a Archivos/imagen/Catálogo, menús Importar/Archivo/Editar/Vista, herramientas compactas que pasan a otra fila si no caben, navegación y edición junto a la imagen. Paneles ajustables/ocultables y restablecibles. Vistas Detalles/Lista/Miniaturas de primera página con número de páginas, rueda para desplazarse, Ctrl+rueda o deslizador para tamaño (64–224), preferencias al cerrar. Carga por celdas visibles, un TIFF por vez y caché máxima de 48 miniaturas. Selección y edición pendientes se comparten entre vistas. No se cambian ni generan miniaturas sobre los archivos fuente.

`tools/smoke_ui.py` comprueba ventanas Tk reales, tamaños 860/1024/1366, herramientas dentro del ancho, imagen con más de la mitad de altura, tres vistas, tamaño con rueda, 240 filas virtuales, cancelación de selección/edición, zoom/paneles y pantallas operativas. Resultado correcto; ampliada después la comprobación a todas las barras de herramientas anidadas (`.development/smoke-ux-final-v05.log`). Una captura de pantalla solo mostró el escritorio y no se aceptó como evidencia visual. Queda revisión humana del aspecto/teclado/escala en oficina.

Resultado final: **181 ejecutadas, 173 aprobadas, 8 omitidas privadas, 0 fallos/errores** en CPython 3.11.4 con Pillow 12.3.0 y 9.5.0. Evidencia: `ensayos/modelo-operativo-v05-final/{resultado,pruebas}.json`, `.development/pillow95-entrega-v05.json`. Ensayo completo con demo, diagnóstico, respaldo, restauración y ventanas correcto. Gramática Python 3.8 comprobada en 46 archivos distribuibles (`.development/gramatica-v05.json`). No equivalen a ejecutar CPython 3.8.10 ni Windows 7.

Entrega: `dist/Digitalizacion-0.5.0-codigo.zip`, manifiesto interno y SHA-256 adyacente, 88 archivos permitidos más manifiesto. No copiar el workspace Mac a oficina: seguir WINDOWS7.md, ensayar en carpeta nueva y revisar copia autorizada antes del primer arranque productivo. Pendientes reales: prueba Windows, teclado/escala/junctions, rendimiento TIFF grandes en 3 GB y conciliación de datos originales que aquí no existen. No hay acceso a producción para ejecutar ese despliegue desde esta sesión.

## 2026-09-22 · Pruebas OSX independientes

A petición del usuario, se preparó `/Users/admin/PruebasDigitalizacionOSX` con entorno virtual y copia de código 0.5.0 independientes. El lanzador permite continuar una demo, crear otra sin borrar las previas o ejecutar el ensayo automático. Acceso en Escritorio `Digitalizacion-Pruebas-OSX.command`. No se abrió producción ni se modificaron sus datos; la preparación inicial solo alteró metadatos Finder ajenos al código. La bitácora propia del laboratorio registra sus sesiones.

## 2026-09-22 · Ampliación 0.6.0 · Listas por folio y varios legajos

Solicitud: listas CSV/Excel con encabezados variables, CURP+folio, catálogos reutilizables, métricas TIFF/páginas en la tabla y filtros mientras se escribe. El usuario aclaró que la lista no suele incluir legajo y una misma CURP puede tener varios expedientes físicos. Decisión: no deducir legajo del consecutivo No.; importar la relación CURP/folio con legajo pendiente y permitir desglosar explícitamente 1,2,3… al cotejar físicos. No usar nombres ni documentos reales en pruebas.

`intake.py`/`intake_ui.py` añaden lectura CSV/XLSX, codificaciones/separadores, detección de encabezado y correspondencia editable, hoja/fila y valores comunes opcionales, vista previa sin escritura y confirmación atómica con snapshot. Lotes por hash/hoja/configuración; origen crudo conservado, filas erróneas pendientes e identidades ya existentes reutilizadas sin sobrescribir su operación. Comprueba cambios de fuente antes de confirmar. Fecha solicitud, Integra y TRABAJADOS permanecen declarados; no acreditan recepción, asignación ni avance. RFC sin CURP válida queda pendiente; XLS antiguo exige conversión.

`register_legajos` conserva el primer elemento pendiente y añade los físicos faltantes, vinculados por CURP. Cada legajo tiene maestro/ciclo/TIFF/conteos separados; `lista_padre_id` conserva el origen de los añadidos. Hay navegación entre legajos en Expedientes y 360. La lista sin legajo aparece en el folio antes de crear un ciclo físico; el alta técnica mantiene recepción pendiente y no permite asignación hasta aceptarse físicamente.

Núcleo 7 (`intake_schema.sql`): directorio, alias, referencias en campos operativos y procedencia de listas. `directory.py` permite alta/consulta/cambio/baja lógica/reactivación; normaliza espacios, acentos y mayúsculas, mantiene IDs y alias al renombrar y respeta los nombres de hechos históricos. Migración enlaza valores anteriores sin borrar/reemplazar texto. `directory_ui.py` añade selectores con mantenimiento dentro del flujo; los formularios restauran la modalidad previa al cerrar un diálogo hijo.

Tabla Expedientes: CURP, nombre, folio, legajo, estado, TIFF activos, páginas, hojas físicas, digitalizador y recepción. Fuera del broche/carpetas se retiran de la presentación principal, no de los datos. Consulta agregada de metadatos y filtros con pausa de 220 ms, parcial/acento indiferente, selección conservada y fechas incompletas guiadas; no hace hashes ni reconstruye Kanban al escribir. El filtrado exacto anterior de servicios continúa por defecto. 360/datasets incluyen directorio, procedencia y legajos relacionados. La demo conserva tres expedientes iniciales y añade un CSV de práctica sin importarlo automáticamente.

## 2026-09-22 · Validación y entrega 0.6.0

19 regresiones nuevas: 17 de importación/directorio/desglose y dos de migración/rollback. Suite en CPython 3.11.4, Pillow 12.3.0 y 9.5.0: **200 ejecutadas, 192 aprobadas, 8 omitidas privadas, 0 fallos/errores** (`.development/v060-suite.json`, `v060-pillow95.json`). Las pruebas WAL indicadas por el usuario y sus regresiones continúan habilitadas y pasan. AST Python 3.8: 51 archivos distribuibles (`gramatica-v06.json`); no ejecución de CPython 3.8.10.

Tk real: filtros parciales sin refresco pesado, importación/vista previa, tres legajos y directorio anidado con retorno modal; se mantiene comprobación de Codificación/miniaturas/Cancelar (`v060-smoke.log`). Ensayo integrado de demo/diagnóstico/respaldo/verificación/restauración/ventanas correcto en `ensayos/listas-v06-final/resultado.json`.

E2E de código archivado 0.5.0 y demo inventada: actualizar conserva SQLite byte por byte; migrar 6→7 conserva todas las filas/columnas previas y TIFF; directorio poblado, importación nueva y tres legajos restaurables desde respaldo completo; retorno posterior rechazado (`e2e-v06-from05.log`). E2E desde 0.3: actualización, retorno de código previo antes de migrar, reaplicación, migración y entrega verificable; retorno posterior rechazado (`e2e-v06-from03.log`). Pruebas sobre candidato con el mismo código de aplicación; documentación final se completa después de esos ensayos. Resguardo previo de desarrollo: `.development/pre-v060-codigo.zip`; ZIP anteriores conservados.

Entrega de código: `dist/Digitalizacion-0.6.0-codigo.zip` y SHA-256 adyacente, con manifiesto interno y sin SQLite/TIFF/demos. README, ACTUALIZACION.html, WINDOWS7, manual de listas, validación y contexto reflejan núcleo 7/TIFF 3. Laboratorio OSX de esta versión en `codigo-0.6.0` y `sesiones-v06`, lanzador `laboratorio-v06.py` y acceso Escritorio `Digitalizacion-Pruebas-OSX-060.command`; se mantienen las sesiones y el acceso 0.5.0. El registro concreto de apertura queda en la bitácora independiente del laboratorio.

Pendientes reales: ensayo Windows 7 SP1/CPython 3.8.10 x64, teclado/escala/junctions, rendimiento con volúmenes de oficina en 3 GB y cotejo autorizado de datos originales. No se tuvo acceso a producción ni se ejecutó allí la actualización. El archivo idéntico/configuración idéntica no reprocesa pendientes; se corrige la fuente o se concilia el elemento explícitamente. Historial y fuentes ambiguas no se fusionan automáticamente.

## 2026-09-23 · 0.6.1 · Recepción práctica e Indicadores BI

Observación del usuario al probar en Mac: aceptar una lista exigía volver a capturar CURP y los valores de las barras no se visualizaban bien. Se confirmó que el formulario anterior abría vacíos CURP/legajo recibidos. Se reemplazó el flujo habitual por selección y cotejo visual; se mantuvo el formulario individual de diferencias con datos precargados.

`reception.py`/`reception_ui.py`: búsqueda CURP/nombre, filtro de estado, selección múltiple/seleccionar visibles, vista previa sin escritura, CURP importada no recapturada, propuesta explícita de legajo común para registros sin legajo, confirmación de físicos, aceptación y creación de ciclos en la misma transacción. Se preservan números conocidos y se pueden añadir otros desde Más acciones. Repetir conserva evidencia y no duplica; un fallo revierte todo el lote. Diferencias, faltantes, extras, identidades repetidas, otro folio, devoluciones y ciclos inactivos se detienen para revisión. Se registra fecha individual faltante sin inventar entrega/acuse global. `Operations._validate_item` permite la transacción externa; su API pública conserva el caso individual. Importación ofrece Continuar con recepción y el préstamo Ver en Expedientes.

`dashboard.py`/`dashboard_ui.py`: cuatro tarjetas principales; seis barras de etapas con cantidad/porcentaje y por aprobar separado de cerrados; cinco coberturas con denominador común; filtros locales compartidos con Expedientes; tabla por folio/digitalizador; detalle navegable a 360; todas las métricas y exportación con filtros/ámbito. Cero resultados muestra Sin base. Aviso global de elementos sin ciclo separado de los totales filtrados. El gráfico anterior usaba posiciones fijas y textos con valores visuales predeterminados; el nuevo reserva espacio para cifras, define contraste y se redibuja por ancho. Textos explicativos ajustan su salto de línea; resumen desplazable con poca altura.

La consulta BI utiliza metadatos agregados y no llama al indicador legado que podía verificar los TIFF mediante `latest/current_validation`. Redimensionar solo dibuja datos en memoria; escribir no reconstruye Kanban. Se muestran estados registrados, sin presentar el panel como certificación de integridad ni sumar entregas/retirados/cifras legadas. Se conservaron los servicios de validación y los consumidores del indicador legado.

Pruebas nuevas: 11 de recepción y 7 de BI. La primera prueba BI de organizar omitía confirmar el conteo físico; corregido únicamente el fixture. Resultado: **218 ejecutadas, 210 aprobadas, 8 omitidas privadas, 0 fallos/errores** en CPython 3.11.4 con Pillow 12.3.0 y 9.5.0 (`.development/v061-suite.json`, `v061-pillow95.json`). Pruebas WAL reportadas siguen habilitadas. AST Python 3.8: 57 archivos. Smoke Tk: aceptar cinco elementos sin recapturar (legajos existentes/pendientes), Cancelar, tres tamaños 880/1024/1366, cifras dentro del canvas, contraste, textos adaptables, sin base al filtrar, detalle y exportación; redimensionado sin consulta y filtrado sin leer TIFF. Mantiene el smoke de Codificación/miniaturas y resto de ventanas (`v061-smoke-delivery.log`).

Ensayo `.development/e2e_v061.py`: código 0.6.0 archivado + demo/lista inventada → 0.6.1 conserva SQLite byte por byte al copiar código y abrir, sin cambio de esquema → aceptación por selección conserva legajo 2 conocido y confirma dos pendientes como 1 → BI/exportación → respaldo/restauración íntegros → recuperación del código bloqueada tras cambiar datos. TIFF previos intactos. Evidencia `e2e-v061.log`, repetición sobre paquete final `e2e-v061-final.log`. Resguardo previo `.development/pre-v061-codigo.zip`; versiones distribuidas anteriores conservadas.

Entrega `dist/Digitalizacion-0.6.1-codigo.zip`, manifiesto/SHA-256, núcleo 7 / TIFF 3 sin migración desde 0.6.0. Actualizados README, ACTUALIZACION.html, WINDOWS7, manual, validación y contexto. Mac usa copia `codigo-0.6.1` y lanzador `laboratorio-v061.py` / acceso `Digitalizacion-Pruebas-OSX-061.command`, permite continuar sesiones-v06 y crear nuevas; conserva código/accesos anteriores. Preparación y apertura se registran en la bitácora independiente del laboratorio. Windows 7/CPython 3.8.10, teclado/escala y rendimiento de oficina continúan pendientes; producción no fue abierta ni actualizada.


## 2026-09-23 · Documentación de uso y actualización guiada 0.6.2

Petición: actualizar ACTUALIZACION, hacer el cambio de versión sencillo y explicarlo a principiantes; documentar el uso completo con ejemplos y casos. Se crean fuentes canónicas `docs/ACTUALIZACION.md` (12 apartados) y `docs/MANUAL_USUARIO.md` (26), con HTML offline generado por `tools/build_docs.py`: índice enlazado en columnas adaptables, tipografía/contraste, retorno al índice, búsqueda del navegador y estilos de impresión. No depende de CDN ni de librerías Markdown. El empaquetador regenera ambas páginas antes de calcular sus hashes. Manual abarca importación, recepción por selección/parcial, faltantes/extras, varios legajos, ciclos históricos, directorio, asignación, conteo/incidencias, codificación y miniaturas, reescaneo/retiro, entregas/versiones, observaciones/aprobación, devolución física, BI, exportación, fuentes legadas, respaldos, Mac e incidencias comunes. Ejemplos exclusivamente sintéticos.

Se añade botón Ayuda a la app y se aclara el mensaje inicial sobre la importación diaria. README y WINDOWS7 priorizan la guía con botones; se mantienen comandos técnicos para administración y recuperación. RESPALDOS/ENSAYO actualizan rutas. OPERACION_V05 se identifica como antecedente y corrige nombres de recepción, condición de asignación del folio completo y exportación BI para evitar instrucciones contradictorias.

`asistente_actualizacion.py` + `ACTUALIZAR.bat`: elegir proyecto existente; comprobar paquete/base y Python/Tk/Pillow; ensayo con datos ficticios fuera del proyecto y paquete; abrir demo; confirmación humana de revisión/cierre; respaldo y actualización mediante `actualizar.py`. Windows usa el entorno local del destino y exige el perfil CPython 3.8.10 x64, Pillow 9.5.0 y Windows 7 SP1 en el informe. En Mac el ensayo se distingue expresamente. Selección diferente, manifiesto cambiado, binario alterado, perfil incorrecto o ensayo fallido bloquean continuar. Subprocesos sin shell, registro de aplicación y tareas con hilo/cola; cierre bloqueado mientras se trabaja. No instala dependencias, no abre producción ni migra por sí mismo. Se conserva intacto el motor de actualización/backup/retorno y las validaciones WAL/SHM. Disposiciones especiales de datos se documentan fuera del asistente automático.

Validación local: 13 nuevas regresiones; **231 ejecutadas, 223 aprobadas, 8 omitidas privadas, 0 fallos/errores**, tanto Pillow 12.3.0 como 9.5.0 en CPython 3.11.4. Evidencias `.development/v062-suite.json` y `v062-pillow95.json`; las dos pruebas WAL reportadas anteriormente siguen pasando y habilitadas. Tk real: asistente a 700/820/1024, trabajo en hilo, bloqueo/cierre; smoke de visor/recepción/BI y Ayuda (`v062-smoke-asistente.log`, `v062-smoke-ui.log`). AST 3.8: 61 archivos; enlaces/anclas de los dos HTML sin errores (`v062-documentos-gramatica.json`). Captura de la guía renderizada en Chrome local inspeccionada; esto no verifica el navegador del equipo Windows.

E2E candidato `.development/e2e_v062.py`: toma ZIP 0.6.1 conservado, crea una instalación ficticia, ejecuta el asistente real (incluido ensayo con ventanas), aplica 0.6.2 con SQLite idéntico y TIFF intactos, verifica/restaura el resguardo a otra raíz y recupera solo código anterior. Se corrigen en el harness inicial los argumentos de `restore_code` (no había defecto en la aplicación). Repetición correcta: `e2e-v062-candidato-corregido.log`. Núcleo 7/TIFF 3 sin migración desde 0.6.1. El contexto resume arquitectura y puntos de continuación. El paquete final lleva manifiesto y SHA-256, sin datos ni entornos locales; versiones anteriores y laboratorio 0.6.1 se conservan. Windows 7, BAT, perfil CPython 3.8.10 real, impresión/navegador, teclado y rendimiento de oficina siguen pendientes. Producción no se ha abierto ni actualizado.

## 2026-09-23 · Kit0.6.3: traslado conservador Windows7 → Windows10

Petición vigente: migración reproducible/reversible a HP Windows10/i5/4GB conservando código0.6.2, datos/historia y Python3.8.10/Pillow9.5; modernización posterior independiente. Se revisaron arquitectura, esquemas reales, 61 Python/8.332 líneas, BAT, backup/restore/update/recovery, TIFF, rutas/manifiestos y docs antes de cambiar código. Se escribió PLAN_MIGRACION_WINDOWS10.md y se conservó snapshot de código en .development. No hay .git ni producción accesible; no se inventa commit/certificación Windows.

Diferencias con la oficina: este repositorio no contenía todavía su fallback de intérprete, la exclusión temprana de auxiliares en data_fingerprint ni el tratamiento correcto de historia absoluta. Se preserva el código exacto capturado del equipo origen por separado del kit, evitando actualizar aplicación durante el traslado. Núcleo7/TIFF3 sin DDL nuevo. Se corrige fingerprint excluyendo WAL/SHM/journal antes de resolve, mantiene iterdump transaccional/backup API. Backup distingue activos estrictos de campos históricos conocidos, normaliza solo en snapshot y registra cambios. Activos con enlaces incluso internos, traversal, externos o hash incorrecto se rechazan. No se suprimen PermissionError globales.

Herramientas nuevas: integrity (runtime, libre disco, SQLite/FK, all-table tipado, hashes/páginas/referencias), portability, transfer (código+datos+manifiestos; normalización demostrada por inversión en copia temporal), migration_acceptance (ensayo del kit y código origen, demo/ventanas, barrera y aceptación), migration_ui (trabajador/cola), runtime, tiff_recovery. Entradas migrar/diagnosticar_integridad/auditar_integridad_tiff. Candidato TIFF externo desde revisión+operación editar completada, solo giro/recorte explícito, exige hash_actual exacto y conserva original/base intactos. Publicaciones a destino exclusivo con marca incompleta hasta final; no reemplazan directorios vacíos ajenos. Destino bloqueado por .restauracion_incompleta hasta ensayo válido y confirmación de congelamiento/revisión. Una demo nunca se etiqueta producción. Arranque permite datos nuevos; cambia de ruta de intérprete solo con bibliotecas iguales, dejando evidencia sin tocar SQLite.

BAT unificados mediante EJECUTAR/PowerShell2/python_runtime/lanzar: prueba real de versión/64bits/Tk/Pillow/libtiff, .venv vacía/rota descartada, ruta configurada, registro y alternativas; no dependencia ciega de PATH ni reintento de una acción operacional fallida. INSTALAR_VISOR prepara entorno local y wheel9.5 verificado, sin paquetes globales. El origen no se reinstala por rutina. Asistente de actualización usa resolutor compartido y acepta también perfil conservador Win10; actualización tradicional sigue independiente y NO maneja aún el destino dividido del traslado.

Pruebas finales en macOS11.7.11 Intel/Darwin20.6: **276 totales,264 aprobadas,12 omitidas,0 fallidas,0 errores**, en cada uno de CPython3.8.10/Pillow9.5.0 (conda-forge/Tcl8.6.13/SQLite3.46/libtiff4.5), CPython3.13.15/Pillow12.3.0 (conda-forge/Tcl8.6.13/SQLite3.53.4/libtiff4.7.1) y CPython3.11.4/Pillow12.3 existente. JSON/logs .development/w10-full3810-final, w10-full31315-final, w10-full3114-final. Omisiones:8 fuentes privadas y4 casos nativos Windows (handles WAL/SHM, bootstrap BAT/PS, SUBST). Es ejecución real de3.8.10 en Mac, no binario Windows. En una corrida preliminar3.8 apareció supuesto ambiental incorrecto en test_missing_target_windows_environment: se corrigió fixture con candidatos explícitamente inexistentes, manteniendo el caso activo.

Smoke legado aplicación/visor/BI/recepción correcto; asistente migración abre a900/1024 y ejecuta auditoría en trabajador sin tocar datos reales. Corpus sintético entre runtimes:4/4 contenidos/páginas/metadata comparada iguales,0/4 SHA de bytes iguales; comparación devuelve2 por discrepancia verdadera. Candidato3.13 además emite aviso de finalización AppendingTiffWriter en smoke pese a completar controles. Se registra deuda y se bloquea modernización productiva; no se silencian alertas ni se reescribe el encoder legado en esta fase.

Ensayo conservador externo /Users/admin/PruebasDigitalizacionMigracion/ensayo-conservador-001: ZIP0.6.2 previo intacto, demo creada con ese código, exportación/normalización/manifiestos, comparación origen, otra raíz/usuario con acentos, restauración, ambas suites, demo/ventanas, aceptación exclusivamente laboratorio, app conservada abre workspace restaurado y TIFF single/multi. Resultado fuenteSQLite idéntica byte a byte, comparación final del origen sin diferencias. Código0.6.2 conservado:231/223/8/0; kit capturado antes de último caso fallback:275/263/12/0. Informe resultado.json y log .development/w10-e2e001.log. Laboratorio OSX061 y sesiones existentes intactos.

Documentos: MIGRACION_WINDOWS10.md/html para principiantes, MIGRACION_TECNICA (arquitectura/formato/invariantes/24casos), MODERNIZACION (matriz/deuda/hoja de ruta), VALIDACION_V063, README/ACTUALIZACION/WINDOWS7/COMPATIBILIDAD/contexto. HTML generado sin red. Rollback antes de nuevas capturas: origen comprobado y congelado; después: preservar backup del destino nuevo y planificar otro traslado, nunca pisarlo con base vieja. Conservación mínima operativa sugerida30d más respaldos restaurables; políticas institucionales prevalecen. Python moderno, OCR, asincronía, empaquetado/CI, DB servidor y Docker siguen posteriores; Docker opcional, no instalado ni requerido.

Entrega: dist/Digitalizacion-0.6.3-codigo.zip, manifiesto y SHA-256 adyacente (sin datos, entornos ni caches). Versiones anteriores conservadas. Pendiente real A–M: exportación/corte de producción Win7, importación/ensayo nativo Win10, keyboard/NTFS/Powershell/TIFF reales/rendimiento4GB y aceptación/rollback de oficina. Windows10 Home/Pro general fuera de soporte requiere verificar edición/cobertura ESU/LTSC; no se cambia el objetivo ni se aplica modernización mayor.


## Respaldo solicitado en GitHub · proyecto Python/Tk0.6.3

Se conectó la carpeta a https://github.com/vicfueagui/digitalizacion. Main remoto ya tenía otra implementación Django y tres commits, último8e15abb; se conserva intacto. Rama independiente respaldo/digitalizacion-tk-0.6.3, con historia propia, para evitar sustituir o mezclar arquitecturas. Primer commit3aeb8cf: snapshot exacto de los144 archivos del manifiesto0.6.3. Segundo commit: exclusiones, atributos de bytes/EOL y documentación del respaldo. Identidad Git local de vicfueagui con noreply comprobado; la configuración global de ejemplo no se utilizó.

Revisados hashes de los144 archivos contra el ZIP entregado, lista explícita y patrones de credenciales; CURP de fixtures/manual identificadas como sintéticas. Excluidos datos SQLite/TIFF/Excel/CSV, entornos, ensayos, logs, documentos originales de entrada y archivos privados. El ZIP0.6.3 y su SHA previo siguen intactos en dist. No se modifica código funcional ni producción; suite previa276/264/12/0/0 por perfil sigue siendo la evidencia funcional. RESPALDO_GITHUB.md documenta clonar la rama, distinguirla de main, generar paquetes y mantener los respaldos de datos por separado.
